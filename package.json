{
  "name": "@joinbox/joinimation",
  "version": "0.2.0",
  "description": "Adds classes to HTML elements when they become visible",
  "main": "src/Joinimation.js",
  "ava": {
    "files": [
      "**/*.unit.js",
      "**/*.integration.js"
    ],
    "require": [
      "esm"
    ]
  },
  "scripts": {
    "test": "npx ava --watch",
    "demo": "npx parcel example/index.html",
    "build": "npx parcel build src/buildScript.js --out-dir build --out-file Joinimation.js --bundle-node-modules --detailed-report"
  },
  "author": "Felix Steiner <felix@joinbox.com>",
  "license": "ISC",
  "devDependencies": {
    "@joinbox/eslint-config-joinbox": "^1.0.8",
    "ava": "^2.4.0",
    "babel-core": "^6.26.3",
    "babel-eslint": "^10.0.3",
    "babel-plugin-syntax-class-properties": "^6.13.0",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "esm": "^3.2.25",
    "jsdom": "^15.2.1",
    "parcel": "^1.12.4"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "events": "^3.0.0"
  }
}
